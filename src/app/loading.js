import React from "react";

const loading = () => {
  return (
    <div
      style={{
        textAlign: "center",
        marginTop: "200px",
      }}
    >
      loading
    </div>
  );
};

export default loading;
